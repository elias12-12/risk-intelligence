"""Authoring a rule: validate it, simulate it, publish it.

`validate.py` is shared by the simulate path (session 2) and the publish path
(session 3) on purpose — WEEK5-PLAN decision 6. The body an admin tests and the
body they save are the same body, checked by the same function, so the tested
thing and the written thing cannot diverge.
"""
